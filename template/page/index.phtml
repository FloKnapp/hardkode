<?php

use Hardkode\Model\Article;

/**
 * @var Article[] $articleList
 * @var Article   $article
 * @var Article   $latestArticle
 */
?>
<?php $this->layout('/layout.phtml') ?>

<?php $this->block('title') ?>
Startseite
<?php $this->endBlock('title') ?>

<?php $this->block('content') ?>

<?php
$latestArticle = array_shift($articleList) ?? [];
?>

<div class="stage">

    <div class="text">
        <div class="date"><?= $this->date($latestArticle->insertedAt)->format('d.m.Y // H:i') . ' Uhr' ?></div>
        <h1><?= $latestArticle->title ?></h1>
    </div>

</div>

<div class="grid">

    <?php foreach ($articleList as $article): ?>

        <div class="box">
            <div class="date"><?= $this->date($article->insertedAt)->format('d.m.Y // H:i') . ' Uhr' ?></div>
            <h3><?= $article->title ?></h3>
        </div>

    <?php endforeach; ?>

</div>

<?php $this->endBlock('content') ?>
